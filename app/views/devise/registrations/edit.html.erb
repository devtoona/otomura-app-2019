<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
<div class="edit-account-page-container-index">
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="edit-account-container">
    <h2 class="edit-profile-title">アカウント情報の編集</h2>
    <div class="new-acount-form-container">
      <div class="new-acount-field-container">
        <%= f.label :email,"メールアドレスを入力" %><br />
        <%= f.email_field :email, class:"frame",autofocus: true, autocomplete: "email",value:"" %>
      </div>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
      <% end %>

      <div class="new-acount-field-container">
        <%= f.label :password,"新しいパスワードを入力" %><br />
        <%= f.password_field :password, class:"frame", autocomplete: "new-password" %>
      </div>

      <div class="new-acount-field-container">
        <%= f.label :password_confirmation,"確認のため、パスワードを再入力" %><br />
        <%= f.password_field :password_confirmation,class:"frame", autocomplete: "new-password" %>
      </div>

      <div class="new-acount-field-container">
        <%= f.label :current_password,"現在のパスワードを入力" %><br />
        <%= f.password_field :current_password, class:"frame", autocomplete: "current-password" %>
      </div>

      <%= link_to "アカウント削除",registration_path(resource_name), data: { confirm: "アカウントを削除しますか？" },class:"account_delete_link",method: :delete %>

      <div class="actions">
        <%= f.submit "更新",class:"update_btn btn" %>
      </div>
    </div>
  </div>
</div>
<% end %>
